class User_Data():
    def __init__(self):
        self.host = '127.0.0.1'  # Standard loopback interface address (localhost)
        self.port = 50010  # Port to listen on (non-privileged ports are > 1023)
        self.programName = 'Chatsy'
        self.background = '#515354'
        self.screenSize = ('1280x720+300+120')
        self.userDatabase = "User_Data.db"
